

import 'package:flutter/material.dart';

class OrangeDeviderWidget extends StatelessWidget {
  const OrangeDeviderWidget({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Container(
      margin: const EdgeInsets.only(left: 10),
      height: 1,
      color: Colors.orange,
    );
  }
}
