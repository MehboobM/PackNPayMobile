name: pack_n_pay
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.10.1

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  pinput: ^6.0.2
  flutter_riverpod: ^2.6.1
  flutter_svg: ^2.0.17
  shared_preferences: ^2.5.2
  easy_localization: ^3.0.7+1
  dio: ^5.8.0+1
  google_fonts: ^8.0.2
  dropdown_button2: ^2.3.9
  toastification: ^3.0.3
  path_provider: ^2.1.5
  open_file: ^3.5.11
  permission_handler: ^12.0.1
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  url_launcher: ^6.2.5
  share_plus: ^12.0.2

  intl: ^0.20.2
  syncfusion_flutter_pdfviewer: ^28.1.33
  image_picker: ^1.1.2
  file_picker: ^8.0.7
  flutter_typeahead: ^6.0.0
  signature: ^5.4.0
  flutter_local_notifications: ^17.1.2
  timezone: ^0.9.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

  # Splash & Icon
  flutter_native_splash: ^2.4.4
  flutter_launcher_icons: ^0.13.1

  # Package rename (dev only)
  change_app_package_name: ^1.1.0


# ---------------- ICON CONFIG ----------------
flutter_icons:
  android: true
  ios: false
  image_path: "assets/images/pack_pay_logo.png"
  min_sdk_android: 21


# ---------------- SPLASH CONFIG ----------------
flutter_native_splash:
  color: "#ffffff"
  image: assets/images/pack_pay_logo.png

  android: true
  android_gravity: center

  ios: true
  ios_content_mode: center
  image_ios: assets/images/pack_pay_logo.png


flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/lang/
    - assets/icons/